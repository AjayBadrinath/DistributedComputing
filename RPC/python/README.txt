1. Run the servercal.go file first and then run the python file
2. Interact with the server using client(py) file
3. Allowed operations [Add/Subtract/Multiply/Divide]<space>num1<space>num2
----Note---
Only integers are allowed and returns an int as the structure i defined in the go file has int64 inputs and returns an int64 format number
